# FINAL IMAGE SETUP

Generate each image using the exact filename in IMAGE-PROMPTS.md and copy it into its specified folder. The corresponding page is already wired to the exact relative path. Do not edit HTML. JPG is the master format; missing images are hidden only at their own slot and cannot break page layout.
